# 2026-07-02 (목)

## 📊 업무 현황
| 상태 | 카테고리 | 업무 내용 | 우선순위 | 난이도 | 소요시간 | 진도 | 예상 완료일 | 비고 |
|-----|-----|-----|-----|-----|-----|-----|-----|-----|
| 진행중 | 자동화시스템개발 | Test Analyzer 개발 : 개발 plan 작성 | 중 | 3h | 100% | - | - |
| 완료 | 자동화테스트운영 | webkeeper playwright 자동화 : 사용자 정의 사이트 수정 TC 관련 이슈 체크, 사용자 정의 사이트 신규 백업 시 오브젝트 중복되는 현상 체크 | 중요 | 하 | 1h | 100% | - | - |
| 진행중 | 자동화시스템개발 | AQA Portal : [hotfix] Stable 탭의 Test Suite 목록이 AQA 팀 외 사용자에게 보이지 않는 문제 수정 | 중 | 1h | 100% | - | - |

---

## 📝 Doodle
- 0701 db 데이터 중에 중복된게 있는데 다시 설치해서 다시 백업해볼까?
  - 진도: update job 멈춰놨고, 일단 사용자 정의사이트가 2개씩 생기는 문제가 있는데, DB에는 1개로 저장되므로 일단은 수동으로 사용자 정의사이트 관련 데이터를 넣어주고 백업하자. 그리고 메모해둔 사용자정의사이트 관련 이슈와 연관되었을 수도 있으니까 일감 먼저 올리자. (maili 장비에서 더블 체크.)
  - trello : 사용자 정의 사이트 수정 관련 이슈 체크, 사용자 정의 사이트 신규 백업 시 오브젝트 중복되는 현상 체크
- Test Analyzer 개발 plan 작성
  - 진도: REQUIRENT.md, DESIGN.md 파일 작성했음. 근데 데이터 수집 및 데이터 소스 대상 중에 "AI의 응답과정(설명 가능한, AI가 답변한 흐름과 근거)를 데이터화 하는 부분이 없어서 추가가 필요할 듯.

---

- webkeeper playwright
  - 유지보수 : 로그조회 저장된 필터 삭제, HTTP 정책 대응행동 요소 변경 관련 개선
  - 일감 올리기 : 사용자 정의 사이트에서 정책 목록 및 삭제 버튼 안나타나는 현상. (그냥 저장된 내용 자체가 그대로 보이지가 않는 현상임)  
  - DB 업데이트 이슈 : 혹시 자동화 테스트 진행하면서 정책 복원 관련 액션때문은 아닌지, 수동체크 및 관련 TC skip 처리 후 확인 필요. 
  - 백업한 DB를 복원할때에 에러 발생. 다음 가이드대로 진행해보기 (https://share.gemini.google/leMKHaxEBwrp)
  - 실장님의 메터 pg_dump 명령어
    ```
    pg_dump -h 192.168.1.10 -p 5432 somansa \
    -U postgres -Fc -b -v -Z 0 \
    -N assets_log_* -N common_log_* \
    -N pi_log_* -N pi_mypc_log_* \
    -N smsdlp_statistiscs_* -N wk_log_* \
    -N tproxy_session_log_* -N mi_log_* \
    -f 백업덤프파일_1
    ```
  - 유리선임님의 메터 pg_dump 명령어
    ```
    pg_dump --host 127.0.0.1 --port 5432 --username "postgres" --format custom --section pre-data --section data --section post-data --column-inserts --no-privileges --verbose --file /root/test.sql "somansa"
    ```
  - 최종 정리된 pg_dump 명령어 (옵션 간 모순 제외, 예외 스키마 추가함.)
    ```
    pg_dump --host 127.0.0.1 --port 5432 --username "postgres" \
      --format custom \
      --no-privileges \
      --verbose \
      --exclude-schema common_log_* \
      --file /root/dbbackup/databaseBackup_260701.dump \
      "somansa"
    ```

- AI analyzer
  - https://share.google/aimode/m0oC2GviWe4Tr9KRr
  - 테스트 결과 폼이나 리포트 안에 테스트 결과에 대한 리뷰를 강제하도록. (그렇지 않으면 테스트가 안돌게?, 뭔가 레포지토리 그룹 권한 수정을 통해서 강제하도록 할까 싶기도 하고.) (추가 아이디어. BDDGen을 gitlab ci에 넣기.)
  - https://chatgpt.com/c/6a42ff45-a50c-83ee-9d06-e458dfb74b29
  - https://chatgpt.com/c/6a435317-f570-83ee-adb6-38e3472922fd (multi-agent)
  - https://chatgpt.com/share/6a4360b8-7584-83e8-b6f3-a1316f6812bd (AI업무 개선 제안)

- qa data platform
  - 진도 : 아래 스킬로 plan을 점검했고, plan-design-review, plan-ceo-review 이 두 스킬을 순서대로 실행하는 것이 필요.
  - 팀장님에게 제안하기 위한 개발 제안서 작성.
  -  review가 포함된 스킬 목록:
    - plan-eng-review (엔지니어링 리뷰)
    - plan-devex-review (개발자/운영자 경험 리뷰)
    - plan-design-review (설계/UX 리뷰)
    - plan-ceo-review (비즈니스/전략 리뷰)
    - review (일반 코드 리뷰)
    - design-review (일반 디자인 리뷰)
    - devex-review (일반 개발자 경험 리뷰)

---

- Potal
  - gitlab-runner 구축
  - 자동화 사용 통계 개발 팀장님 리뷰 2 
    - 메터 확인
    - 실행 이력에서 SUITE 부분 정렬
    - 그리고 배포 시 sync 뭐시기 스크립트 배포용 db에서 실행시켜야함..
  - !!(중요) 배포 시 반드시 기억할 것!!
    ```
    # 3. team_members 테이블에 department 칼럼 추가
    docker exec -it test-db-postgres-1 psql -U aqa_portal -d aqa_portal -c "ALTER TABLE team_members ADD COLUMN IF NOT EXISTS department VARCHAR(100)"

    # 4. 컨테이너 안에서 아래 실행하여 team_members 테이블 갱신 (#3과 이어짐)
    python sync_ad_members.py

    # 5. AD_BIND_USER와 AD_BIND_PASSWORD 값을 주입
      # 컨테이너 밖의 서버 호스트에서 실행해야 합니다!
        cd /var/services/homes/admin/aqa-portal-dev
        echo -e "\nAD_BIND_USER=aqa_team" >> .env
        echo -e "AD_BIND_PASSWORD=Aqarhksfl1!" >> .env
    ```
  - 내용
    feat: Playwright 자동화 사용 통계에 조직별 및 개인별 조회 기능 추가 
    feat(statistics): 웹 자동화 사용 통계 UI 개선 및 버그 수정 
    chore(script): 통계 누락 사용자 AD 동기화 스크립트 추가 
    fix(statistics): 개인/부서 필터 및 빈 데이터 예외 처리 UI 일관성 개선 

---
[gitlab-runner 설정 진도 (하단 내용에 대해 아직 진행 못함.)]
✦ 제공해주신 CI 출력 로그와 .gitlab-ci.yml 파이프라인 설정을 바탕으로
  현재 배포가 실패한 원인을 분석한 결과입니다.

  크게 두 가지 문제가 확인되며, 두 번째 문제가 파이프라인을 실패하게
  만든 결정적인 원인입니다.

  1. Docker Compose 버전/명령어 호환성 문제 (경고성)
   * 로그:

   1     [CMD] echo '***' | sudo -S docker compose version
   2     [ERR] Password: docker: 'compose' is not a docker command.
   * 원인 분석:
      배포 헬퍼 스크립트(cicd_deploy_helper.py)에서 최신 플러그인 방식인
  docker compose(v2, 띄어쓰기) 명령을 먼저 시도했으나, 접속한 배포
  서버(Synology NAS)에는 구버전인 독립형 docker-compose(v1, 붙여쓰기)만
  설치되어 있어 발생한 에러입니다. 스크립트 내부적으로 이를 감지하고
  docker-compose로 Fallback하여 다음 단계를 진행했으므로, 이 자체로
  파이프라인이 중단되진 않았습니다.

  2. IMAGE_NAME 환경 변수 누락으로 인한 Pull 실패 (핵심 원인)
   * 로그:

   1     msg="The \"IMAGE_NAME\" variable is not set. Defaulting to a
     blank string."
   2     Error response from daemon: no such image: :015fe4fa: invalid
     reference format
   3     [오류] CI/CD 배포 중 실패: 명령 실패 (exit 1)
   * 원인 분석:
       1. 원격 서버에서 docker-compose pull 명령이 실행될 때,
          docker-compose.yml 내부에 선언된
          ${IMAGE_NAME}:${DOCKER_IMAGE_TAG} 형태의 이미지를 참조하려고
          시도했습니다.
       2. 로그를 보면 DOCKER_IMAGE_TAG 변수는 이번 커밋의 Short SHA인
          015fe4fa로 정상적으로 치환되었습니다.
       3. 그러나 앞부분의 IMAGE_NAME 변수가 비어있는 문자열로 인식되면서
          최종 조합된 이미지 이름이 :015fe4fa라는 잘못된 형식(Invalid
          Reference)이 되어 도커 데몬이 이미지를 찾지 못해 다운로드에
          실패했습니다.
   * 발생 이유 추정:
      .gitlab-ci.yml 스크립트를 보면 echo
  "IMAGE_NAME=$CI_REGISTRY/aqa-portal" >> $ENV_FILE_CONTENT 명령어를
  통해 .env 파일에 변수를 추가하고 있습니다.
      하지만 GitLab 프로젝트 설정에 컨테이너 레지스트리가 비활성화되어
  있어 기본 제공되는 $CI_REGISTRY 변수 값이 비어있거나, 기존에 주입된
  원본 파일($DEV_ENV_FILE) 내에 빈 IMAGE_NAME= 값이 덮어씌워져 도커
  컴포즈가 원격 서버에서 환경 변수를 올바르게 읽어오지 못한 상태입니다.

  💡 요약
  "배포할 Docker 이미지의 경로(IMAGE_NAME)를 원격 서버의 도커 컴포즈가
  알지 못해 잘못된 주소(:015fe4fa)로 다운로드를 시도하다가 실패한
  상태"입니다. .gitlab-ci.yml의 환경변수 주입 로직이나 GitLab Variable
  설정을 점검하여 .env 파일에 유효한 IMAGE_NAME 값이 정상적으로
  기록되는지 확인이 필요합니다.

---

- Portal 관련
  - gitlab-runner 구축 관련
    - dind 방식 구축 한계로, 호스트 시스템 구축으로 전환.
    - 호스트 시스템 구축 전환에 따른 배포 관련 파일 수정 (.gitlab-ci.yml, docker-compose.yml, docker-compose.dev.yml, Dockerfile)
    - todolist 참고 --> https://gemini.google.com/share/5beccfd9953e

- webkeeper playwright 유지보수
  - cm에서 다음과 같이 모듈 동작 체크 (추후 패키지 업데이트에 넣기 위함)
    ```
    [root@auto-wk-update ~]# vi /somansa/cm/bin/cmcontrol.sh
    [root@auto-wk-update ~]# ps -ef | grep -E "WKServer|AgentManager|NetworkAgent" | grep -vE 'log|grep' | awk '{print $2}'| xargs -r kill -9
    [root@auto-wk-update ~]# /somansa/cm/bin/cmcontrol.sh 19
    [root@auto-wk-update ~]# ps -ef | grep -E "WKServer|AgentManager|NetworkAgent" | grep -vE 'log|grep'
    [root@auto-wk-update ~]# ps -ef | grep -E "WKServer|AgentManager|NetworkAgent" | grep -vE 'log|grep'
    [root@auto-wk-update ~]# ps -ef | grep -E "WKServer|AgentManager|NetworkAgent" | grep -vE 'log|grep'^C
    [root@auto-wk-update ~]# tail -f /somansa/cm/tomcat/logs/cm.log | grep cmcontrol
    [2026-06-17 07:38:51] INFO  [cm.commonsupport.service.ControlServiceImpl:486] - scriptshell : /somansa/cm/bin/cmcontrol.sh 19 START
    │ Command : /bin/sh -c sudo /somansa/cm/bin/cmcontrol.sh 19 START
    [2026-06-17 07:39:05] INFO  [cm.commonsupport.service.ControlServiceImpl:486] - scriptshell : /somansa/cm/bin/cmcontrol.sh 20 START
    │ Command : /bin/sh -c sudo /somansa/cm/bin/cmcontrol.sh 20 START
    ```
  - /somansa/webkeeper/conf/WKConfigReg.xml의 누락 옵션 (MacAddrForUid) 문제로 특정 웹/넷앱에 대한 로깅이 안되고 있는건 아닌지 추가 체크 필요 (Remi와의 메터 확인.)
  - wkserver, agentmanager, networkagent 재시작 하는 것을 패키지 업데이트 쪽에 넣어야함 (근데 networkagent가 계속 죽어서 완료하지 못했었음.)
  - 이 일감도 확인해보면 NetworkAgent 가 동작 안하는 원인에 대해서 조금은 알수도있을지도.. : http://redmine.somansa.com/redmine/issues/157035
    
---

- macos 자동화관련
  - 추후
    - 다른 인원들이 원격접속하기 어려우니, 공식적으로 원격 관련 TC는 빼는게 좋겠다고 팀장님께 말씀 드리기?
    - 실 내 인원들이 모두 접속 가능한 ip 대역으로 변경하기? (10.203.50.X ??)
    - bench03 macos 화면이 확장이 아니라 복제?로 설정할 수 있는지 확인
    - bench-all job 트리거 시 각 벤치 job이 트리거 안되는듯..?

---

- 팀장님 요청 -> 커널 업데이트 자동화
  - https://mattermost.somansa.com/aqateam/pl/aq3ggb8inbnoddqc945jkfnruh
  - 커널 업데이트 알림이 가고, 다음날 새벽에 커널 업데이트가 되도록 (자동화 테스트 전에, 업무시간에 커널이 업데이트 된 장비를 메뉴얼로도 체크해볼 수 있다.)
  - 커널은 버전 업데이트 자동화 장비쪽에서 다운로드 받을 수 있을것 같다. (외부망이 되니까.)
  - gemini : https://gemini.google.com/share/b79a41c96d18

---

- 신규 LLM 서버 질문 명령어
curl -s http://10.226.50.2/v1/chat/completions -H "Content-Type: application/json" -d "{\"model\": \"gemma-4\", \"messages\": [{\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": \"자기소개 시작\"}]}], \"max_tokens\": 2048}"
