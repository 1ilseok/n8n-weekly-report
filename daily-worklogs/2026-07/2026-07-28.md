# 2026-07-28 (화)

## 📊 업무 현황
| 상태 | 카테고리 | 업무 내용 | 우선순위 | 난이도 | 소요시간 | 진도 | 예상 완료일 | 비고 |
|-----|-----|-----|-----|-----|-----|-----|-----|-----|
| 완료 | 자동화테스트운영 | sv-i testcomplete 스크립트 유지보수 : [로그 조회 > 파일 검사] 테이블 헤더 위치 변경에 따른 관련 TC 유지보수 | 일반 | 하 | 1h | 100% | - | - |
| 진행중 | 자동화시스템개발 | Test Healer 개발 : Jenkinsfile 파이프라인 및 타임아웃/리포트 설정 최적화 : 파이프라인 전체 타임아웃(90분) 및 Healer 실행 스테이지 타임아웃(100분) 확장으로 후처리 버퍼(20분) 확보, 타임아웃 예외 발생 시 post 블록의 리포트 생성을 보장하기 위해 try-catch 격리 및 call 키워드 보완 // Playwright Healer 에이전트 (.agent.md) 프롬프트 고도화 : 로컬 LLM 32K 컨텍스트 제약을 고려한 토큰 절약 지침 통합 및 다른 spec.ts 파일 열람 엄격 금지, Step 5 fallback 디버깅 로직 개선(error-context.md 활용) 및 환경 오류/파일 누락 발생 시 조기 중단(ABORT) 조건 명시, 코드 수정 시 쉘 명령어(sed/awk) 사용 금지 및 edit 도구 기반 3~5줄 Context 포함 유일성 검증 규칙 강제, 검증 성공 여부에 따른 최종 리포트 기재 규칙 정립 // 추론 파서 스크립트(extract_thinking.js) 버그 수정 및 리포팅 강화 : stopReason 속성의 'toolUse' 카멜케이스 예외 처리 누락 버그 수정, stopReason, errorMessage, compaction 감지 및 토큰 한도 초과(max_tokens) 중단 사유 가시성 강화, 세션 최종 상태 및 요약(Final Summary) 자동 출력 기능 추가 | 중 | 4h | 100% | - | 

---

## 📝 Doodle
- sv-i testcomplete 유지보수
  -> 이거 리포트 보니까 추가 수정이 필요하다.
  - 내용 : [로그 조회 > 파일 검사] 테이블 헤더 위치 변경에 따른 관련 TC 유지보수 

- (추후) sv-i 일감 등재 (Primary Key 칼럼 불일치 문제) 

- db comparision 일감 올리기

- 팀장님께 제안
  - version-alert : kernel 버전만 개발해서, 대시보드를 포탈에 넣기
  - 앱 다운로드 및 버전 관리 시스템 : 포털의 시스템 테스트 영역에 넣기
  - 다연이가 개발한거 : 포털의 시스템 테스트 영역에 넣기
  - 다 외부망이 필요해서, 

- Test Healer
  ```
  - Jenkinsfile 파이프라인 및 타임아웃/리포트 설정 최적화                                         
    - 파이프라인 전체 타임아웃(90분) 및 Healer 실행 스테이지 타임아웃(100분) 확장으로 후처리 버퍼(20분) 확보
    - 타임아웃 예외 발생 시 post 블록의 리포트 생성을 보장하기 위해 try-catch 격리 및 call 키워드 보완
    - Review Changes 스테이지의 git diff 검사 대상을 tests 폴더로 범위 제한 설정
    - publishHTML 게시 시 워크스페이스 전체 복사 병목 방지를 위한 전용 디렉터리(html-report) 생성 구조 적용                                                                                               
  - Playwright Healer 에이전트 지시사항(.agent.md) 프롬프트 고도화                                
    - 로컬 LLM 32K 컨텍스트 제약을 고려한 토큰 절약 지침 통합 및 참고용 스펙 파일 열람 엄격 금지 
    - Step 5 fallback 디버깅 로직 개선(error-context.md 활용) 및 환경 오류/파일 누락 발생 시 조 
      기 중단(ABORT) 조건 명시
    - 코드 수정 시 쉘 명령어(sed/awk) 사용 금지 및 edit 도구 기반 3~5줄 Context 포함 유일성 검  
      증 규칙 강제
      - 테스트 검증 단계를 위한 --retries=0 옵션 적용 및 검증 성공 여부에 따른 최종 리포트 기재 규칙 정립                                                                                 
                                                                                              
  - 추론 파서 스크립트(extract_thinking.js) 버그 수정 및 리포팅 강화                              
    - stopReason 속성의 'toolUse' 카멜케이스 예외 처리 누락 버그 수정                           
    - stopReason, errorMessage, compaction 감지 및 토큰 한도 초과(max_tokens) 중단 사유 가시성  
      강화                                                                                  
    - 세션 최종 상태 및 요약(Final Summary) 자동 출력 기능 추가  
  ```
  - 개인 개발용 하네스 시스템 고도화 (git 링크: )


---

- feeder 개발 플랜 (회의 내용)
  * **원칙 : ES 데이터는 관련 데이터가 같이 생성되어야 함.**
  1. ES 데이터 분석 : 제품 담당자별, ES 데이터의 관련 데이터 항목 분석 및 공유. (정책, 사용자, 부서 등)
  2. Skill : spec.ts 파일과 함께 feeder shema, es dump 파일 등을 같이 구성해서 zip 파일을 만들어줌 (사용자가 script를 만들 때 대상 서버의 ip도 같이 전달되며, 해당 장비에 ES 데이터가 존재해야하며, 해당 장빙에서 ES 데이트를 가져와야 함.)
  3. AQA팀 Stable 스크립트에 feeder 적용 및 업로드
  4. Stable 70% 정도 작업 완료 시 Portal (원용) 및 Agent (일석) 쪽 개발 시작
  * **참고**
    - Agent에 feeder 를 주입하는 방식.
    - 포털에서 실행 시, Playwright 실행 전 대상 TC들의 모든 ES 데이터를 모아 ES 데이터 주입하는 것을 가장 먼저 일괄 실행해야함.
    - Stable TC가 아닌, 타 팀에서 작성한 TC들은 ES 데이터 생성은 지원하지만, 정책 오브젝트 생성 feeder 스키마 생성은 지원하지 않음. 
    - 그러나 타 팀에서 작성한 TC들은 cleanup 로직은 지원함.

```

---

- macos 자동화 개선
  - 개선예정
    - pvi_load.sh, pvi_unload.sh 콘솔 출력결과로 나오는 info 항목 전부에 대해서 체크

- AQA Portal 관련 공지 (버전 올려서 내일 하는게 좋겠음)
  ```
  다시보니까 접속정보 프리셋에서, 비밀번호가 원래 들었던 프리셋이 비밀번호가 사라졌 
  는데?


  Interpreting User Input

  I'm currently focused on the user's input, specifically the observation about the 
  missing password. It appears this directly relates to how the frontend handles    
  decrypted passwords received from the backend API, and a possible configuration   
  issue with the existing preset. I'm exploring the implications of this.

  네, 정확히 그 증상입니다.

  서버의 ENCRYPT_KEY가 변경되면, DB에 저장되어 있던 기존 암호화된 비밀번호를 복호화
  하지 못하게 됩니다. 이때 백엔드는 오류를 내는 대신 빈 문자열("")을 프론트엔드로
  보내기 때문에, UI 상에서도 비밀번호가 감쪽같이 사라진(비어 있는) 것으로 보이게 됩
  니다.

  해결 방법: 해당 프리셋의 비밀번호 칸에 다시 비밀번호를 입력하고 저장하여, 현재 서
  버의 새로운 키로 다시 암호화되도록 갱신해주시면 됩니다.
  ```

--- 

- AQA Portal 개발 추후 작업
    ```
     ### 스크립트 작성 완료 (scripts/docker_image_cleanup.sh)

 핵심 동작 논리:
 1. <none> 이미지 처리: docker image prune -f 명령을 통해 컨테이너와 연결되지 않은 쓸모없는 
    레이어와 <none> 이미지를 최우선으로 일괄 삭제합니다.
 2. 동일 ID 다중 태그 처리: 출력해주신 예시를 보면 여러 태그(015fe4fa, 1c51cee9 등)가 동일  
    한 이미지 ID(38952f9a063f)를 공유하고 있습니다. 스크립트가 docker rmi로 과거 태그들을   
    지우면, 실제 용량(366MB)은 최후의 1개 태그가 지워질 때만 반환되며, 이전까지는 논리적인  
    '태그(링크) 해제'만 수행하므로 매우 안전합니다.
 3. 안전장치 (Safety):
     - latest 태그는 삭제 대상에서 자동 제외(grep -v ':latest')됩니다.
     - docker rmi -f (강제 옵션)를 쓰지 않으므로, 혹여나 현재 서버에서 구동 중인 이미지가   
       삭제 대상에 포함되어도 Docker 데몬 단위에서 삭제가 거부되어 무중단 서비스가 보장됩니 
       다.

 ────────────────────────────────────────────────────────────────────────────────

 ### 서버(10.206.10.197 등) 적용 가이드

 서버에 접속하신 뒤 다음 절차에 따라 스크립트를 주기적으로 실행하도록 설정하십시오.

 1. 스크립트 권한 부여 및 테스트

 ```bash
   chmod +x /opt/aqa-portal/scripts/docker_image_cleanup.sh
   # 수동으로 먼저 1회 실행하여 예상대로 동작하는지 확인
   /opt/aqa-portal/scripts/docker_image_cleanup.sh
 ```

 2. Crontab 등록 (자동화)

 ```bash
   sudo crontab -e
 ```

 편집기 하단에 아래 내용을 추가합니다 (매주 일요일 새벽 3시에 실행 및 로그 기록).

 ```text
   0 3 * * 0 /opt/aqa-portal/scripts/docker_image_cleanup.sh >> /var/log/docker_cleanup.log 
 2>&1
 ```
    ```
---

- Test Healer 개발
  - 진도
    - mcp timeout이 걸려서 chatgpt에 문의중 : https://chatgpt.com/share/6a588f17-5da0-83e8-b524-5ed9bc358bbc
    - 프롬프트에 dlpcenter 프로젝트를 정확히 지정해줘야 할 듯. (아예 실행 명령어를 명시적으로 넣기) (dlpcenter)

- 하네스엔지니어링 구성
  - https://share.gemini.google/t8ctUunvMM5z

---

- (추후) 다연이가 만든 거랑 인턴이 만든거랑 기존 version-alert 시스템 통합 (+ 커널 버전 알림 추가)

- Test Analyzer 개발 : GraphDB 기반 소스코드 지식베이스 시스템 개발
  - PHASE8 어떻게 할지 결정 못함. 관련 내용 이 파일에 저장 : PHASE8_ARCHITECTURE_OPTIONS.md
     
---

- webkeeper playwright
  - 유지보수 : 로그조회 저장된 필터 삭제, HTTP 정책 대응행동 요소 변경 관련 개선
  - 일감 올리기 : 사용자 정의 사이트에서 정책 목록 및 삭제 버튼 안나타나는 현상. (그냥 저장된 내용 자체가 그대로 보이지가 않는 현상임)  
  - DB 업데이트 이슈 : 혹시 자동화 테스트 진행하면서 정책 복원 관련 액션때문은 아닌지, 수동체크 및 관련 TC skip 처리 후 확인 필요. 
  - 백업한 DB를 복원할때에 에러 발생. 다음 가이드대로 진행해보기 (https://share.gemini.google/leMKHaxEBwrp)
  - 실장님의 메터 pg_dump 명령어
    ```
    pg_dump -h 192.168.1.10 -p 5432 somansa \
    -U postgres -Fc -b -v -Z 0 \
    -N assets_log_* -N common_log_* \
    -N pi_log_* -N pi_mypc_log_* \
    -N smsdlp_statistiscs_* -N wk_log_* \
    -N tproxy_session_log_* -N mi_log_* \
    -f 백업덤프파일_1
    ```
  - 유리선임님의 메터 pg_dump 명령어
    ```
    pg_dump --host 127.0.0.1 --port 5432 --username "postgres" --format custom --section pre-data --section data --section post-data --column-inserts --no-privileges --verbose --file /root/test.sql "somansa"
    ```
  - 최종 정리된 pg_dump 명령어 (옵션 간 모순 제외, 예외 스키마 추가함.)
    ```
    pg_dump --host 127.0.0.1 --port 5432 --username "postgres" \
      --format custom \
      --no-privileges \
      --verbose \
      --exclude-schema common_log_* \
      --file /root/dbbackup/databaseBackup_260701.dump \
      "somansa"
    ```

- AI analyzer
  - https://share.google/aimode/m0oC2GviWe4Tr9KRr
  - 테스트 결과 폼이나 리포트 안에 테스트 결과에 대한 리뷰를 강제하도록. (그렇지 않으면 테스트가 안돌게?, 뭔가 레포지토리 그룹 권한 수정을 통해서 강제하도록 할까 싶기도 하고.) (추가 아이디어. BDDGen을 gitlab ci에 넣기.)
  - https://chatgpt.com/c/6a42ff45-a50c-83ee-9d06-e458dfb74b29
  - https://chatgpt.com/c/6a435317-f570-83ee-adb6-38e3472922fd (multi-agent)
  - https://chatgpt.com/share/6a4360b8-7584-83e8-b6f3-a1316f6812bd (AI업무 개선 제안)

- qa data platform
  - 진도 : 아래 스킬로 plan을 점검했고, plan-design-review, plan-ceo-review 이 두 스킬을 순서대로 실행하는 것이 필요.
  - 팀장님에게 제안하기 위한 개발 제안서 작성.
  -  review가 포함된 스킬 목록:
    - plan-eng-review (엔지니어링 리뷰)
    - plan-devex-review (개발자/운영자 경험 리뷰)
    - plan-design-review (설계/UX 리뷰)
    - plan-ceo-review (비즈니스/전략 리뷰)
    - review (일반 코드 리뷰)
    - design-review (일반 디자인 리뷰)
    - devex-review (일반 개발자 경험 리뷰)

---

- Potal
  - !!(중요) 배포 시 반드시 기억할 것!!
    ```
    # 3. team_members 테이블에 department 칼럼 추가
    docker exec -it test-db-postgres-1 psql -U aqa_portal -d aqa_portal -c "ALTER TABLE team_members ADD COLUMN IF NOT EXISTS department VARCHAR(100)"

    # 4. 컨테이너 안에서 아래 실행하여 team_members 테이블 갱신 (#3과 이어짐)
    python sync_ad_members.py

    # 5. AD_BIND_USER와 AD_BIND_PASSWORD 값을 주입
      # 컨테이너 밖의 서버 호스트에서 실행해야 합니다!
        cd /var/services/homes/admin/aqa-portal-dev
        echo -e "\nAD_BIND_USER=aqa_team" >> .env
        echo -e "AD_BIND_PASSWORD=Aqarhksfl1!" >> .env
    ```
  - 내용
    feat: Playwright 자동화 사용 통계에 조직별 및 개인별 조회 기능 추가 
    feat(statistics): 웹 자동화 사용 통계 UI 개선 및 버그 수정 
    chore(script): 통계 누락 사용자 AD 동기화 스크립트 추가 
    fix(statistics): 개인/부서 필터 및 빈 데이터 예외 처리 UI 일관성 개선 
---

- macos 자동화관련
  - 추후
    - 다른 인원들이 원격접속하기 어려우니, 공식적으로 원격 관련 TC는 빼는게 좋겠다고 팀장님께 말씀 드리기?
    - 실 내 인원들이 모두 접속 가능한 ip 대역으로 변경하기? (10.203.50.X ??)
    - bench03 macos 화면이 확장이 아니라 복제?로 설정할 수 있는지 확인
    - bench-all job 트리거 시 각 벤치 job이 트리거 안되는듯..?

---

- 팀장님 요청 -> 커널 업데이트 자동화
  - https://mattermost.somansa.com/aqateam/pl/aq3ggb8inbnoddqc945jkfnruh
  - 커널 업데이트 알림이 가고, 다음날 새벽에 커널 업데이트가 되도록 (자동화 테스트 전에, 업무시간에 커널이 업데이트 된 장비를 메뉴얼로도 체크해볼 수 있다.)
  - 커널은 버전 업데이트 자동화 장비쪽에서 다운로드 받을 수 있을것 같다. (외부망이 되니까.)
  - gemini : https://gemini.google.com/share/b79a41c96d18

---

- 신규 LLM 서버 질문 명령어
curl -s http://10.226.50.2/v1/chat/completions -H "Content-Type: application/json" -d "{\"model\": \"gemma-4\", \"messages\": [{\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": \"자기소개 시작\"}]}], \"max_tokens\": 2048}"
