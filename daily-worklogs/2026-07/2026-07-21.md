# 2026-07-21 (화)

## 📊 업무 현황
| 상태 | 카테고리 | 업무 내용 | 우선순위 | 난이도 | 소요시간 | 진도 | 예상 완료일 | 비고 |
|-----|-----|-----|-----|-----|-----|-----|-----|-----|
| 진행중 | 자동화시스템개발 | AQA Portal : 프론트엔드 빌드 및 배포 환경 안정화 (Vite 환경변수(VITE_BASE_URL) 주입 및 Docker 정적 파일(404) 라우팅 오류 수정), gitlab runner CI/CD 파이프라인 최적화 및 보안 개선(운영 배포 트리거 기준 변경(태그 → main), 배포 스크립트 비밀번호 노출 및 타임아웃 버그 수정), 빌드 리소스 및 워크스페이스 정리 로직 통합(CI 권한 충돌 우회, node_modules, 잉여 도커 이미지 및 불필요한 레거시 파일 정리 자동화 추가), README 갱신 | 중 | 4h | 100% | - | - |
| 진행중 | 자동화시스템개발 | Test Healer 개발 : 테스트 결과(junit.xml) 인라인 파싱 기반 AI 힐링 워크플로우 최적화(토큰 절약을 위한 실패 케이스 타겟팅), BDD 아키텍처 맞춤형 spec.ts 파일 역추적, 단일 프로젝트(--project) 임시 타겟팅, test_run tool 사용하지 않고 원본 직접 실행을 통한 검증 효율화, Jenkins 파이프라인 작업 Plan 작성 | 중 | 3h | 100% | - | 

---

## 📝 Doodle
- AQA Portal 개발
  - 내용
    - 프론트엔드 빌드 및 배포 환경 안정화: Vite 환경변수(VITE_BASE_URL) 주입 및 Docker 정적 파일(404) 라우팅 오류 수정
    - CI/CD 파이프라인 최적화 및 보안 개선: 운영 배포 트리거 기준 변경(태그 → main), 배포 스크립트 비밀번호 노출 및 타임아웃 버그 수정
    - 빌드 리소스 및 워크스페이스 정리 로직 통합: CI 권한 충돌 우회, node_modules, 잉여 도커 이미지 및 불필요한 레거시 파일 정리 자동화 추가 및 README 갱신

- Test Healer 
 [웹키퍼 자동화 테스트 AI 힐링 시스템 구축 요약]                                                                                      
 - 테스트 결과(junit.xml) 인라인 파싱 기반 AI 힐링 워크플로우 최적화 (실패 
   케이스 타겟팅 및 LLM 토큰 절약)                                         
 - BDD 아키텍처 맞춤형 역추적(Back-tracking) 및 원본 소스코드(.spec.ts) 복 
   구 로직 적용 (자동 생성 파일 수정 방지)                                 
 - 단일 프로젝트(--project) 임시 지정 및 원본 직접 실행을 통한 CLI 검증 리소스  
   효율화                                                                  
 - Jenkins CI/CD 비대화형(Headless) 파이프라인 연동 및 사내 로컬 LLM 통합  
   환경 구축 (세션 추적 로깅 포함)  

- 진도
  - 아래 내용을 보면, 너무 추론해서 수정하는데, 이 부분 그대로 ai에게 넘겨주고 프롬프트를 수정할 필요가 있음.
    ```
    
C:\Users\somansa\Desktop\Webkeeper_10.1>pi @.github/agents/playwright-test-healer.agent.md --session-id "healer-123123" --name "Healer Build 123123" --print "이전 테스트가 실패했습니다. 첨부된 지시사항(워크플로우)에 따라 test-results/junit.xml을 분석하고 힐링을 시작하세요."
Warning: No project session found with id 'healer-123123'; creating a new session with that id.
요청하신 `test-results/junit.xml` 분석 및 실패한 Playwright 테스트에 대한 코드 수정(Healing) 작업을 완료했습니다. 

각 테스트의 실패 원인(Root Cause)과 해결 방법(Remedy)은 다음과 같습니다.

### 1. `03.incidents.spec.ts` (로그 조회 웹 기능 확인)
- **증상:** `Test timeout of 70000ms exceeded.`
- **원인:** 넷앱 필터 버튼 클릭 시, 화면 내의 "필터를 선택하십시오" 로딩/안내 엘리먼트(`<li class="select_period">`)가 Pointer Event를 가로채어(Intercept) 클릭이 반복적으로 실패하고 타임아웃이 발생함.
- **조치:** `tests/dlpcenter/page-based/03.incidents.spec.ts` 파일에서 해당 엘리먼트에 대한 `.click()` 메서드에 `{ force: true }` 옵션을 추가하여 강제 클릭하도록 수정. (수정 후 `dlpcenter` 환경에서 정상 통과 확인)

### 2. `07.dbupdate.spec.ts` (DB 업데이트 기능 확인 - page-based)
- **증상:** `Error: page.waitForTimeout: Test timeout of 240000ms exceeded.`
- **원인:** 테스트 내에서 `await page.waitForTimeout(240000)`(4분 대기)를 수행하는데, `test.setTimeout(240000)`으로 전체 테스트 타임아웃도 정확히 4분으로 설정되어 있어, 대기 외의 다른 스텝을 수행하는 과정에서 타임아웃 경계값에 도달하여 실패함.
- **조치:** `tests-backup/dlpcenter/page-based/07.dbupdate.spec.ts` 파일에서 `test.setTimeout(300000)`(5분)으로 테스트 타임아웃 마진을 충분히 확보하도록 연장함.

### 3. `04.policy.spec.ts` (정책 규칙 관리 - 사용자 정의 사이트)
- **증상:** `Error: expect(locator).toBeVisible() failed` (중복된 정보입니다 메시지 박스 미노출)
- **원인:** 중복된 호스트를 저장하는 시나리오에서, 작성된 코드가 의도(`userdef-site-duplication.com/`)와 다르게 두 번째 입력 시 `userdef-site-duplication.com/2`를 입력하고 있어, 실제로는 중복 데이터가 아닌 신규 데이터로 처리됨.
- **조치:** `tests-backup/dlpcenter/testcomplete/04.policy.spec.ts` 파일에서 두 번째 호스트 입력값을 주석의 의도와 동일하게 `userdef-site-duplication.com/`으로 변경하여 중복 검증 로직이 정상 작동하도록 수정함.

### 4. `07.dbupdate.spec.ts` (DB 업데이트 기능 확인 - testcomplete)
- **증상:** `Error: expect(locator).toHaveText(expected) failed` (성공 텍스트 불일치)
- **원인:** 환경적인 요인 또는 백엔드 장애로 인해 업데이트 상태 목록을 조회할 때 "목록 가져오기 실패"가 출력되고 있음. 
- **조치:** `tests-backup/dlpcenter/testcomplete/07.dbupdate.spec.ts` 파일의 해당 테스트에 워크플로우 지침을 따라 `test.fixme()` 처리를 적용하고, "현재 업데이트 상태에 '성공' 대신 '목록 가져오기 실패'가 표시되는 문제가 발생하여 fixme 처리함"이라는 주석을 추가하여 무의미한 실패를 방지함.

### 5. `08.manage.spec.ts` (정책 생성 및 백업)
- **증상:** `Error: expect(locator).toBeVisible() failed` (정책을 백업 완료했습니다 메시지 박스 타임아웃)
- **원인:** 백업 진행 시간이 기본 타임아웃인 5000ms(5초)를 초과하여 발생함. 기존 코드에는 `await page.waitForTimeout(15000)` 하드코딩이 있었으나 시스템 부하에 따라 15초를 넘길 가능성이 높음.
- **조치:** `tests-backup/dlpcenter/testcomplete/08.manage.spec.ts` 파일에서 완료 메시지 박스를 기다리는 Assert 구문에 `.toBeVisible({ timeout: 60000 })` 옵션을 명시적으로 부여하여, 불필요한 하드코딩 대기를 줄이고 최대 60초까지 유연하게 기다리도록 안정성을 강화함. 

**참고사항:** 
현재 시스템 환경에 일부 인증 토큰(`storageState` 만료) 및 BDD Fixture 경로 문제가 존재하여 `dlpcenter-dbicc` 등의 서브 프로젝트 환경에서는 CLI 테스트가 로그인 화면으로 리다이렉트 되는 현상이 있습니다. 이는 코드의 결함이 아닌 환경 구성 이슈이므로, 원본 `.spec.ts`의 로직 수정 및 구조적 결함에 대한 치유(Healing) 작업에 집중하여 완료하였습니다.

C:\Users\somansa\Desktop\Webkeeper_10.1>
C:\Users\somansa\Desktop\Webkeeper_10.1>pi @.github/agents/playwright-test-healer.agent.md --session-id "healer-456" --name "Healer Build 456" --verbose --print "이전 테스트가 실패했습니다. 첨부된 지시사항(워크플로우)에 따라 test-results/junit.xml을 분석하고 힐링을 시작하세요."
Warning: No project session found with id 'healer-456'; creating a new session with that id.
지시된 워크플로우에 따라 `test-results/junit.xml` 분석 및 테스트 힐링을 완료했습니다.

### 1. Identify Failing Tests & Back-track
`junit.xml` 분석 결과, 아래 테스트들에서 실패가 발생했음을 확인했습니다.
- `03.incidents.feature.spec.js`: "필터 입력하여 넷앱/HTTPS 로그 조회 결과 확인한다" (Timeout/Pointer Intercept 오류)
- `07.dbupdate.feature.spec.js`: "업데이트 수행 기능을 확인한다" 외 1건
- `04.policy.feature.spec.js`: "중복된 호스트 저장 시..."
- `08.manage.feature.spec.js`: "정책 생성 후 백업하고..."

현재 `tests/` 디렉토리 내에 원본 코드가 존재하는 파일은 `tests/dlpcenter/page-based/03.incidents.spec.ts` 1건으로 확인되어, 해당 파일에 대한 디버깅 및 수정을 진행했습니다.

### 2. Error Investigation
- **에러 원인**: `03.incidents.spec.ts` 실행 중, 넷앱 필터 버튼 클릭 시 `<strong class="c_6974a0">필터를 선택하십시오.</strong>` 엘리먼트가 포인터 이벤트를 가로채어(Intercept) 타임아웃(70000ms)이 발생했습니다.
- **분석 내용**: 검색 키워드 입력 후 `press('Enter')`를 실행한 직후, 이전 동작에서 열려있던 기간 선택 팝업이나 레이어가 완전히 닫히지 않았거나 UI 상태가 안정화되지 않은 상태에서 다음 클릭을 시도하여 발생한 동기화 문제로 판단되었습니다.

### 3. Code Remediation
해당 원본 파일(`tests/dlpcenter/page-based/03.incidents.spec.ts`)에서 `press('Enter')` 이후 UI 렌더링 및 팝업 해제가 완료되도록 대기 시간(`waitForTimeout(1000)`)을 추가했습니다. 이 변경사항은 해당 파일 내에 동일한 로직을 수행하는 두 시나리오 모두에 적용되었습니다.

**수정 내용 (요약)**:
```typescript
  /* And 검색어 입력란에 "사용자 이름":자동화 로깅용 사용자를 입력한다 */
  await page.locator('input[name="keyword"]').fill('"사용자 이름":자동화 로깅용 사용자');
  await page.locator('input[name="keyword"]').press('Enter');
  await page.waitForTimeout(1000); // UI 안정화 및 팝업 닫힘 대기 시간 추가

  /* And 넷앱 필터를 클릭하고, 키워드 입력란에 검색포털 입력 후 해당 체크박스를 선택한다 */
```

### 4. CLI Verification
Playwright CLI(`npx playwright test`)를 통해 BDD 제너레이터 환경변수(`PLAYWRIGHT_MODE=test`)를 설정하여 수정한 원본 `.spec.ts` 파일을 직접 검증했습니다.

- **명령어**:
  `PLAYWRIGHT_MODE=test npx playwright test tests/dlpcenter/page-based/03.incidents.spec.ts -g "TC_WK_0020_Incidents_NetAppsHttps_Search" --project=dlpcenter`
- **결과**: `1 passed (19.6s)` - 패스 확인
- **추가 검증**: 동일 파일 내 영향을 받는 `TC_WK_0021_Incidents_NetAppsHttps_Export` 테스트 또한 `1 passed (20.0s)`로 정상 통과함을 확인했습니다.

*(참고: `07.dbupdate`, `04.policy` 등의 다른 에러 발생 파일들은 현재 타겟 디렉토리(`tests/`) 내에 존재하지 않아, 본 힐링 대상에 포함된 `03.incidents.spec.ts`에 대한 조치만 완료되었습니다.)*

    ```


---

- macos 자동화 개선
  - 개선예정
    - pvi_load.sh, pvi_unload.sh 콘솔 출력결과로 나오는 info 항목 전부에 대해서 체크

- AQA Portal 관련 공지 (버전 올려서 내일 하는게 좋겠음)
```
 다시보니까 접속정보 프리셋에서, 비밀번호가 원래 들었던 프리셋이 비밀번호가 사라졌 
 는데?


 Interpreting User Input

 I'm currently focused on the user's input, specifically the observation about the 
 missing password. It appears this directly relates to how the frontend handles    
 decrypted passwords received from the backend API, and a possible configuration   
 issue with the existing preset. I'm exploring the implications of this.

 네, 정확히 그 증상입니다.

 서버의 ENCRYPT_KEY가 변경되면, DB에 저장되어 있던 기존 암호화된 비밀번호를 복호화
 하지 못하게 됩니다. 이때 백엔드는 오류를 내는 대신 빈 문자열("")을 프론트엔드로
 보내기 때문에, UI 상에서도 비밀번호가 감쪽같이 사라진(비어 있는) 것으로 보이게 됩
 니다.

 해결 방법: 해당 프리셋의 비밀번호 칸에 다시 비밀번호를 입력하고 저장하여, 현재 서
 버의 새로운 키로 다시 암호화되도록 갱신해주시면 됩니다.
```

--- 

- AQA Portal 개발 추후 작업
    ```
     ### 스크립트 작성 완료 (scripts/docker_image_cleanup.sh)

 핵심 동작 논리:
 1. <none> 이미지 처리: docker image prune -f 명령을 통해 컨테이너와 연결되지 않은 쓸모없는 
    레이어와 <none> 이미지를 최우선으로 일괄 삭제합니다.
 2. 동일 ID 다중 태그 처리: 출력해주신 예시를 보면 여러 태그(015fe4fa, 1c51cee9 등)가 동일  
    한 이미지 ID(38952f9a063f)를 공유하고 있습니다. 스크립트가 docker rmi로 과거 태그들을   
    지우면, 실제 용량(366MB)은 최후의 1개 태그가 지워질 때만 반환되며, 이전까지는 논리적인  
    '태그(링크) 해제'만 수행하므로 매우 안전합니다.
 3. 안전장치 (Safety):
     - latest 태그는 삭제 대상에서 자동 제외(grep -v ':latest')됩니다.
     - docker rmi -f (강제 옵션)를 쓰지 않으므로, 혹여나 현재 서버에서 구동 중인 이미지가   
       삭제 대상에 포함되어도 Docker 데몬 단위에서 삭제가 거부되어 무중단 서비스가 보장됩니 
       다.

 ────────────────────────────────────────────────────────────────────────────────

 ### 서버(10.206.10.197 등) 적용 가이드

 서버에 접속하신 뒤 다음 절차에 따라 스크립트를 주기적으로 실행하도록 설정하십시오.

 1. 스크립트 권한 부여 및 테스트

 ```bash
   chmod +x /opt/aqa-portal/scripts/docker_image_cleanup.sh
   # 수동으로 먼저 1회 실행하여 예상대로 동작하는지 확인
   /opt/aqa-portal/scripts/docker_image_cleanup.sh
 ```

 2. Crontab 등록 (자동화)

 ```bash
   sudo crontab -e
 ```

 편집기 하단에 아래 내용을 추가합니다 (매주 일요일 새벽 3시에 실행 및 로그 기록).

 ```text
   0 3 * * 0 /opt/aqa-portal/scripts/docker_image_cleanup.sh >> /var/log/docker_cleanup.log 
 2>&1
 ```
    ```
---

- Test Healer 개발
  - 진도
    - mcp timeout이 걸려서 chatgpt에 문의중 : https://chatgpt.com/share/6a588f17-5da0-83e8-b524-5ed9bc358bbc
    - 프롬프트에 dlpcenter 프로젝트를 정확히 지정해줘야 할 듯. (아예 실행 명령어를 명시적으로 넣기) (dlpcenter)

- 하네스엔지니어링 구성
  - https://share.gemini.google/t8ctUunvMM5z

---

- (추후) 다연이가 만든 거랑 인턴이 만든거랑 기존 version-alert 시스템 통합 (+ 커널 버전 알림 추가)

- Test Analyzer 개발 : GraphDB 기반 소스코드 지식베이스 시스템 개발
  - PHASE8 어떻게 할지 결정 못함. 관련 내용 이 파일에 저장 : PHASE8_ARCHITECTURE_OPTIONS.md
     
---

- webkeeper playwright
  - 유지보수 : 로그조회 저장된 필터 삭제, HTTP 정책 대응행동 요소 변경 관련 개선
  - 일감 올리기 : 사용자 정의 사이트에서 정책 목록 및 삭제 버튼 안나타나는 현상. (그냥 저장된 내용 자체가 그대로 보이지가 않는 현상임)  
  - DB 업데이트 이슈 : 혹시 자동화 테스트 진행하면서 정책 복원 관련 액션때문은 아닌지, 수동체크 및 관련 TC skip 처리 후 확인 필요. 
  - 백업한 DB를 복원할때에 에러 발생. 다음 가이드대로 진행해보기 (https://share.gemini.google/leMKHaxEBwrp)
  - 실장님의 메터 pg_dump 명령어
    ```
    pg_dump -h 192.168.1.10 -p 5432 somansa \
    -U postgres -Fc -b -v -Z 0 \
    -N assets_log_* -N common_log_* \
    -N pi_log_* -N pi_mypc_log_* \
    -N smsdlp_statistiscs_* -N wk_log_* \
    -N tproxy_session_log_* -N mi_log_* \
    -f 백업덤프파일_1
    ```
  - 유리선임님의 메터 pg_dump 명령어
    ```
    pg_dump --host 127.0.0.1 --port 5432 --username "postgres" --format custom --section pre-data --section data --section post-data --column-inserts --no-privileges --verbose --file /root/test.sql "somansa"
    ```
  - 최종 정리된 pg_dump 명령어 (옵션 간 모순 제외, 예외 스키마 추가함.)
    ```
    pg_dump --host 127.0.0.1 --port 5432 --username "postgres" \
      --format custom \
      --no-privileges \
      --verbose \
      --exclude-schema common_log_* \
      --file /root/dbbackup/databaseBackup_260701.dump \
      "somansa"
    ```

- AI analyzer
  - https://share.google/aimode/m0oC2GviWe4Tr9KRr
  - 테스트 결과 폼이나 리포트 안에 테스트 결과에 대한 리뷰를 강제하도록. (그렇지 않으면 테스트가 안돌게?, 뭔가 레포지토리 그룹 권한 수정을 통해서 강제하도록 할까 싶기도 하고.) (추가 아이디어. BDDGen을 gitlab ci에 넣기.)
  - https://chatgpt.com/c/6a42ff45-a50c-83ee-9d06-e458dfb74b29
  - https://chatgpt.com/c/6a435317-f570-83ee-adb6-38e3472922fd (multi-agent)
  - https://chatgpt.com/share/6a4360b8-7584-83e8-b6f3-a1316f6812bd (AI업무 개선 제안)

- qa data platform
  - 진도 : 아래 스킬로 plan을 점검했고, plan-design-review, plan-ceo-review 이 두 스킬을 순서대로 실행하는 것이 필요.
  - 팀장님에게 제안하기 위한 개발 제안서 작성.
  -  review가 포함된 스킬 목록:
    - plan-eng-review (엔지니어링 리뷰)
    - plan-devex-review (개발자/운영자 경험 리뷰)
    - plan-design-review (설계/UX 리뷰)
    - plan-ceo-review (비즈니스/전략 리뷰)
    - review (일반 코드 리뷰)
    - design-review (일반 디자인 리뷰)
    - devex-review (일반 개발자 경험 리뷰)

---

- Potal
  - !!(중요) 배포 시 반드시 기억할 것!!
    ```
    # 3. team_members 테이블에 department 칼럼 추가
    docker exec -it test-db-postgres-1 psql -U aqa_portal -d aqa_portal -c "ALTER TABLE team_members ADD COLUMN IF NOT EXISTS department VARCHAR(100)"

    # 4. 컨테이너 안에서 아래 실행하여 team_members 테이블 갱신 (#3과 이어짐)
    python sync_ad_members.py

    # 5. AD_BIND_USER와 AD_BIND_PASSWORD 값을 주입
      # 컨테이너 밖의 서버 호스트에서 실행해야 합니다!
        cd /var/services/homes/admin/aqa-portal-dev
        echo -e "\nAD_BIND_USER=aqa_team" >> .env
        echo -e "AD_BIND_PASSWORD=Aqarhksfl1!" >> .env
    ```
  - 내용
    feat: Playwright 자동화 사용 통계에 조직별 및 개인별 조회 기능 추가 
    feat(statistics): 웹 자동화 사용 통계 UI 개선 및 버그 수정 
    chore(script): 통계 누락 사용자 AD 동기화 스크립트 추가 
    fix(statistics): 개인/부서 필터 및 빈 데이터 예외 처리 UI 일관성 개선 
---

- macos 자동화관련
  - 추후
    - 다른 인원들이 원격접속하기 어려우니, 공식적으로 원격 관련 TC는 빼는게 좋겠다고 팀장님께 말씀 드리기?
    - 실 내 인원들이 모두 접속 가능한 ip 대역으로 변경하기? (10.203.50.X ??)
    - bench03 macos 화면이 확장이 아니라 복제?로 설정할 수 있는지 확인
    - bench-all job 트리거 시 각 벤치 job이 트리거 안되는듯..?

---

- 팀장님 요청 -> 커널 업데이트 자동화
  - https://mattermost.somansa.com/aqateam/pl/aq3ggb8inbnoddqc945jkfnruh
  - 커널 업데이트 알림이 가고, 다음날 새벽에 커널 업데이트가 되도록 (자동화 테스트 전에, 업무시간에 커널이 업데이트 된 장비를 메뉴얼로도 체크해볼 수 있다.)
  - 커널은 버전 업데이트 자동화 장비쪽에서 다운로드 받을 수 있을것 같다. (외부망이 되니까.)
  - gemini : https://gemini.google.com/share/b79a41c96d18

---

- 신규 LLM 서버 질문 명령어
curl -s http://10.226.50.2/v1/chat/completions -H "Content-Type: application/json" -d "{\"model\": \"gemma-4\", \"messages\": [{\"role\": \"user\", \"content\": [{\"type\": \"text\", \"text\": \"자기소개 시작\"}]}], \"max_tokens\": 2048}"
